<UserControl x:Class="NAutoSuite.UI.Controls.CylinderControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="80"
             d:DesignWidth="220">

    <Border Background="#2D2D30"
            BorderBrush="#3E3E42"
            BorderThickness="1"
            CornerRadius="8"
            Padding="12">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <DockPanel Grid.Row="0">
                <TextBlock Text="{Binding CylinderName, RelativeSource={RelativeSource AncestorType=UserControl}}"
                           Foreground="#B0B0B0"
                           FontSize="12"
                           FontWeight="SemiBold"/>

                <!-- Manual control buttons -->
                <StackPanel Orientation="Horizontal"
                            HorizontalAlignment="Right"
                            DockPanel.Dock="Right"
                            Visibility="{Binding ControlButtonsVisibility, RelativeSource={RelativeSource AncestorType=UserControl}}">
                    <Button Content="◀"
                            FontSize="10"
                            Width="24" Height="20"
                            Margin="0,0,4,0"
                            ToolTip="Retract"
                            Command="{Binding RetractCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                            Style="{StaticResource CylinderButtonStyle}"/>
                    <Button Content="▶"
                            FontSize="10"
                            Width="24" Height="20"
                            ToolTip="Extend"
                            Command="{Binding ExtendCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                            Style="{StaticResource CylinderButtonStyle}"/>
                </StackPanel>
            </DockPanel>

            <!-- Cylinder visualization -->
            <Grid Grid.Row="1"
                  Margin="0,8,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Cylinder body -->
                <Grid Grid.Column="0">
                    <!-- Cylinder housing -->
                    <Border Background="#1E1E1E"
                            BorderBrush="#3E3E42"
                            BorderThickness="1"
                            CornerRadius="4"
                            Height="28">
                        <Grid>
                            <!-- Retracted position indicator -->
                            <Border Background="{Binding RetractedBrush, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                    Width="8"
                                    CornerRadius="2"
                                    HorizontalAlignment="Left"
                                    Margin="4,4"/>

                            <!-- Extended position indicator -->
                            <Border Background="{Binding ExtendedBrush, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                    Width="8"
                                    CornerRadius="2"
                                    HorizontalAlignment="Right"
                                    Margin="4,4"/>

                            <!-- Piston rod -->
                            <Border x:Name="PistonRod"
                                    Background="#2196F3"
                                    Height="12"
                                    CornerRadius="2"
                                    HorizontalAlignment="Left"
                                    Margin="16,0,0,0"
                                    Width="{Binding PistonWidth, RelativeSource={RelativeSource AncestorType=UserControl}}">
                                <Border.Effect>
                                    <DropShadowEffect ShadowDepth="0"
                                                      BlurRadius="4"
                                                      Opacity="0.5"
                                                      Color="#2196F3"/>
                                </Border.Effect>
                            </Border>
                        </Grid>
                    </Border>
                </Grid>

                <!-- Status -->
                <StackPanel Grid.Column="1"
                            VerticalAlignment="Center"
                            Margin="12,0,0,0"
                            Width="60">
                    <TextBlock Text="{Binding StatusText, RelativeSource={RelativeSource AncestorType=UserControl}}"
                               Foreground="{Binding StatusBrush, RelativeSource={RelativeSource AncestorType=UserControl}}"
                               FontSize="11"
                               FontWeight="SemiBold"
                               HorizontalAlignment="Center"/>
                </StackPanel>
            </Grid>
        </Grid>
    </Border>

    <UserControl.Resources>
        <Style x:Key="CylinderButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#3E3E42"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="3">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#505050"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#2196F3"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
</UserControl>
