<UserControl x:Class="NAutoSuite.UI.Controls.NumericDisplayControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="100"
             d:DesignWidth="180">

    <Border Background="{Binding Background, RelativeSource={RelativeSource AncestorType=UserControl}, TargetNullValue=#2D2D30}"
            BorderBrush="#3E3E42"
            BorderThickness="1"
            CornerRadius="8"
            Padding="16">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Title -->
            <TextBlock Grid.Row="0"
                       Text="{Binding Title, RelativeSource={RelativeSource AncestorType=UserControl}}"
                       Foreground="#B0B0B0"
                       FontSize="12"
                       FontWeight="SemiBold"/>

            <!-- Value display -->
            <DockPanel Grid.Row="1"
                       VerticalAlignment="Center"
                       Margin="0,8,0,0">
                <!-- Unit -->
                <TextBlock Text="{Binding Unit, RelativeSource={RelativeSource AncestorType=UserControl}}"
                           Foreground="#6E6E6E"
                           FontSize="{Binding UnitFontSize, RelativeSource={RelativeSource AncestorType=UserControl}}"
                           FontWeight="Normal"
                           VerticalAlignment="Bottom"
                           Margin="6,0,0,4"
                           DockPanel.Dock="Right"
                           Visibility="{Binding UnitVisibility, RelativeSource={RelativeSource AncestorType=UserControl}}"/>

                <!-- Prefix -->
                <TextBlock Text="{Binding Prefix, RelativeSource={RelativeSource AncestorType=UserControl}}"
                           Foreground="#6E6E6E"
                           FontSize="{Binding UnitFontSize, RelativeSource={RelativeSource AncestorType=UserControl}}"
                           FontWeight="Normal"
                           VerticalAlignment="Bottom"
                           Margin="0,0,6,4"
                           DockPanel.Dock="Left"
                           Visibility="{Binding PrefixVisibility, RelativeSource={RelativeSource AncestorType=UserControl}}"/>

                <!-- Value -->
                <TextBlock Text="{Binding DisplayValue, RelativeSource={RelativeSource AncestorType=UserControl}}"
                           Foreground="{Binding ValueForeground, RelativeSource={RelativeSource AncestorType=UserControl}}"
                           FontSize="{Binding ValueFontSize, RelativeSource={RelativeSource AncestorType=UserControl}}"
                           FontWeight="Bold"
                           FontFamily="Consolas"
                           VerticalAlignment="Center"
                           HorizontalAlignment="{Binding ValueAlignment, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
            </DockPanel>
        </Grid>
    </Border>
</UserControl>
