<Window x:Class="NAutoSuite.UI.Controls.Dialogs.ModernMessageBox"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Message"
        WindowStyle="None"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        Background="Transparent"
        AllowsTransparency="True"
        Width="500"
        SizeToContent="Height"
        ShowInTaskbar="False">

    <Border Background="#161B22" BorderBrush="#30363D" BorderThickness="1" CornerRadius="8">
        <Grid Margin="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="60"/>
                <!-- Header -->
                <RowDefinition Height="Auto"/>
                <!-- Content -->
                <RowDefinition Height="70"/>
                <!-- Buttons -->
            </Grid.RowDefinitions>

            <!-- Header -->
            <Border Grid.Row="0" Background="#0D1117" CornerRadius="8,8,0,0" BorderBrush="#21262D" BorderThickness="0,0,0,1">
                <Grid Margin="20,0">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <!-- Icon -->
                        <Border x:Name="IconBorder" Width="32" Height="32" CornerRadius="6" Margin="0,0,15,0">
                            <Viewbox Width="20" Height="20">
                                <Canvas Width="24" Height="24">
                                    <Path x:Name="IconPath" Fill="White"/>
                                </Canvas>
                            </Viewbox>
                        </Border>
                        <!-- Title -->
                        <TextBlock x:Name="TitleText" FontSize="18" FontWeight="SemiBold" Foreground="#C9D1D9" VerticalAlignment="Center"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Content/Message -->
            <Border Grid.Row="1" Padding="30,25">
                <TextBlock x:Name="MessageText"
                           FontSize="15"
                           Foreground="#8B949E"
                           TextWrapping="Wrap"
                           LineHeight="24"/>
            </Border>

            <!-- Buttons -->
            <Border Grid.Row="2" Background="#0D1117" BorderBrush="#21262D" BorderThickness="0,1,0,0" CornerRadius="0,0,8,8">
                <StackPanel x:Name="ButtonPanel" Orientation="Horizontal" HorizontalAlignment="Right" Margin="20,0">
                    <!-- Buttons will be added dynamically -->
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</Window>
