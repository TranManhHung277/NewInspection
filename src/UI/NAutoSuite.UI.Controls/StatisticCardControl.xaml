<UserControl x:Class="NAutoSuite.UI.Controls.StatisticCardControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="100"
             d:DesignWidth="180">

    <Border Background="#2D2D30"
            BorderBrush="#3E3E42"
            BorderThickness="1"
            CornerRadius="8"
            Padding="16">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header with icon and title -->
            <DockPanel Grid.Row="0">
                <!-- Icon -->
                <Border Width="32" Height="32"
                        CornerRadius="6"
                        Background="{Binding IconBackground, RelativeSource={RelativeSource AncestorType=UserControl}}"
                        Margin="0,0,10,0"
                        Visibility="{Binding IconVisibility, RelativeSource={RelativeSource AncestorType=UserControl}}">
                    <TextBlock Text="{Binding Icon, RelativeSource={RelativeSource AncestorType=UserControl}}"
                               FontFamily="Segoe MDL2 Assets"
                               FontSize="16"
                               Foreground="{Binding IconForeground, RelativeSource={RelativeSource AncestorType=UserControl}}"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center"/>
                </Border>

                <!-- Title -->
                <TextBlock Text="{Binding Title, RelativeSource={RelativeSource AncestorType=UserControl}}"
                           Foreground="#B0B0B0"
                           FontSize="12"
                           FontWeight="SemiBold"
                           VerticalAlignment="Center"/>
            </DockPanel>

            <!-- Value -->
            <StackPanel Grid.Row="1"
                        Orientation="Horizontal"
                        VerticalAlignment="Center"
                        Margin="0,8,0,0">
                <TextBlock Text="{Binding DisplayValue, RelativeSource={RelativeSource AncestorType=UserControl}}"
                           Foreground="{Binding ValueForeground, RelativeSource={RelativeSource AncestorType=UserControl}}"
                           FontSize="{Binding ValueFontSize, RelativeSource={RelativeSource AncestorType=UserControl}}"
                           FontWeight="Bold"
                           FontFamily="Consolas"/>
                <TextBlock Text="{Binding Unit, RelativeSource={RelativeSource AncestorType=UserControl}}"
                           Foreground="#6E6E6E"
                           FontSize="12"
                           VerticalAlignment="Bottom"
                           Margin="4,0,0,3"
                           Visibility="{Binding UnitVisibility, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
            </StackPanel>

            <!-- Trend/Change indicator -->
            <StackPanel Grid.Row="2"
                        Orientation="Horizontal"
                        Margin="0,8,0,0"
                        Visibility="{Binding TrendVisibility, RelativeSource={RelativeSource AncestorType=UserControl}}">
                <TextBlock Text="{Binding TrendIcon, RelativeSource={RelativeSource AncestorType=UserControl}}"
                           FontFamily="Segoe MDL2 Assets"
                           FontSize="10"
                           Foreground="{Binding TrendBrush, RelativeSource={RelativeSource AncestorType=UserControl}}"
                           VerticalAlignment="Center"/>
                <TextBlock Text="{Binding TrendText, RelativeSource={RelativeSource AncestorType=UserControl}}"
                           Foreground="{Binding TrendBrush, RelativeSource={RelativeSource AncestorType=UserControl}}"
                           FontSize="11"
                           Margin="4,0,0,0"
                           VerticalAlignment="Center"/>
                <TextBlock Text="{Binding TrendDescription, RelativeSource={RelativeSource AncestorType=UserControl}}"
                           Foreground="#6E6E6E"
                           FontSize="10"
                           Margin="6,0,0,0"
                           VerticalAlignment="Center"/>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
