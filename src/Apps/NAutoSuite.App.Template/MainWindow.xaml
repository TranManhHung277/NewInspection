<Window x:Class="NAutoSuite.App.Template.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:controls="clr-namespace:NAutoSuite.UI.Controls;assembly=NAutoSuite.UI.Controls"
        mc:Ignorable="d"
        Title="NAutoSuite - Machine Control" Height="600" Width="1000"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/NAutoSuite.UI.Themes;component/Theme.Dark.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Style="{StaticResource Card}" Margin="0,0,0,20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock Text="Machine Control Panel" FontSize="24" FontWeight="Bold"/>
                    <TextBlock Text="Template Application" FontSize="14"
                               Foreground="{StaticResource TextSecondaryBrush}" Margin="0,4,0,0"/>
                </StackPanel>

                <controls:MachineStateControl Grid.Column="1"
                                            State="{Binding MachineState}"
                                            VerticalAlignment="Center"/>
            </Grid>
        </Border>

        <!-- Main Content -->
        <Border Grid.Row="1" Style="{StaticResource Card}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="Machine Status" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,16"/>

                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <Border Background="{StaticResource SurfaceBrush}" Padding="16" CornerRadius="4">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <TextBlock Grid.Row="0" Grid.Column="0" Text="State:" Margin="0,0,16,8"
                                         Foreground="{StaticResource TextSecondaryBrush}"/>
                                <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding MachineState}"
                                         FontWeight="SemiBold" Margin="0,0,0,8"/>

                                <TextBlock Grid.Row="1" Grid.Column="0" Text="Title:" Margin="0,0,16,8"
                                         Foreground="{StaticResource TextSecondaryBrush}"/>
                                <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding Title}" Margin="0,0,0,8"/>

                                <TextBlock Grid.Row="2" Grid.Column="0" Text="Busy:" Margin="0,0,16,0"
                                         Foreground="{StaticResource TextSecondaryBrush}"/>
                                <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding IsBusy}"/>
                            </Grid>
                        </Border>

                        <TextBlock Text="Quick Start Guide:" FontSize="14" FontWeight="SemiBold" Margin="0,16,0,8"/>
                        <TextBlock TextWrapping="Wrap" Foreground="{StaticResource TextSecondaryBrush}">
                            1. Click 'Initialize' to connect hardware<LineBreak/>
                            2. Click 'Start' to begin operation<LineBreak/>
                            3. Click 'Stop' to halt operation<LineBreak/>
                            4. Click 'Reset' to return to initial state
                        </TextBlock>
                    </StackPanel>
                </ScrollViewer>
            </Grid>
        </Border>

        <!-- Control Buttons -->
        <Border Grid.Row="2" Style="{StaticResource Card}" Margin="0,20,0,0">
            <UniformGrid Rows="1" Columns="4">
                <Button Content="Initialize" Style="{StaticResource PrimaryButton}"
                        Command="{Binding InitializeCommand}" Margin="0,0,8,0"/>
                <Button Content="Start" Style="{StaticResource SuccessButton}"
                        Command="{Binding StartCommand}" Margin="4,0,4,0"/>
                <Button Content="Stop" Style="{StaticResource WarningButton}"
                        Command="{Binding StopCommand}" Margin="4,0,4,0"/>
                <Button Content="Reset" Style="{StaticResource OutlineButton}"
                        Command="{Binding ResetCommand}" Margin="8,0,0,0"/>
            </UniformGrid>
        </Border>
    </Grid>
</Window>
