<UserControl x:Class="PickAndPlace.Views.SettingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="800" d:DesignWidth="1920">
    <Grid Margin="20">
        <StackPanel>
            <TextBlock Text="Settings" FontSize="28" FontWeight="Bold"
                       Foreground="White" Margin="0,0,0,20"/>
            <Border Style="{StaticResource Card}">
                <StackPanel>
                    <TextBlock Text="Machine Configuration" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,12"/>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="200"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Grid.Column="0" Text="Machine Number:" Margin="0,8"/>
                        <TextBox Grid.Row="0" Grid.Column="1" Style="{StaticResource ModernTextBox}"
                                 Text="{Binding MachineNumber}" Margin="0,8"/>

                        <TextBlock Grid.Row="1" Grid.Column="0" Text="Project Name:" Margin="0,8"/>
                        <TextBox Grid.Row="1" Grid.Column="1" Style="{StaticResource ModernTextBox}"
                                 Text="{Binding ProjectName}" Margin="0,8"/>
                    </Grid>
                </StackPanel>
            </Border>
        </StackPanel>
    </Grid>
</UserControl>
